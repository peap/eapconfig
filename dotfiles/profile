###
# Setup non-Bash things.
###

_configdir="${HOME}/eapconfig/dotfiles"

source_if_exists () {
    [[ -e "$1" ]] && source "$1"
}

source_if_exists "${_configdir}/envvar"
source_if_exists "${_configdir}/paths"
